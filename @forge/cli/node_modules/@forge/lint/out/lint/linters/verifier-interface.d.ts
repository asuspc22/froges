import { LintResultRule, LintClass } from '../linter-interface';
import { ManifestSchema as Manifest } from '@forge/manifest';
export declare class BaseLintIssueVerifier {
    protected environment: string;
    protected manifest: Manifest;
    constructor(environment: string, manifest: Manifest);
    protected getLintClass(): LintClass;
}
export interface LintIssueVerifier<T> {
    process(input: T): Promise<LintResultRule[]>;
}
//# sourceMappingURL=verifier-interface.d.ts.map