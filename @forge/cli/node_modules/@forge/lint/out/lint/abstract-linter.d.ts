import { LintInput, LintResult, LinterInterface, LintLogger } from './linter-interface';
export declare abstract class AbstractLinter implements LinterInterface {
    protected readonly logger: LintLogger;
    protected constructor(logger: LintLogger);
    abstract batchExecuteImpl(asts?: LintInput[]): Promise<LintResult[]>;
    batchExecute(asts?: LintInput[]): Promise<LintResult[]>;
    abstract bootstrap(): Promise<void>;
}
//# sourceMappingURL=abstract-linter.d.ts.map