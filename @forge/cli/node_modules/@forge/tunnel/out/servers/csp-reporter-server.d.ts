import { Express } from 'express';
import { Logger } from '@forge/cli-shared';
import NodeCache from 'node-cache';
export declare class CspReporterServer {
    private readonly port;
    private readonly logger;
    private readonly cache;
    private readonly app;
    private server;
    private static TTL_IN_SECONDS;
    constructor(port: number, logger: Logger, cache: NodeCache);
    getApp(): Express;
    start(): Promise<void>;
    stop(): Promise<void>;
}
//# sourceMappingURL=csp-reporter-server.d.ts.map